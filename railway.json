{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev && npm run build && mkdir -p dist/scripts && cp scripts/*.sql dist/scripts/ 2>/dev/null; cp scripts/ES.txt dist/scripts/ 2>/dev/null; true"
  },
  "deploy": {
    "startCommand": "node dist/scripts/seed-ciudades.js && node dist/src/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
